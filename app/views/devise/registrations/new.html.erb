<%= render 'shared/navbar_devise' %>
<div class="container mb-3" style="padding-top: 100px;">
  <div class="row">
    <div class="col-3"></div>
    <div class="col-lg-6 col-md-6 col-sm-6">
      <div class="card card-register", style="border-radius: 10px">

        <h2>Registro de nuevo usuario</h2>

        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= f.error_notification %>
          <div class="form-inputs">
            <%= f.input :first_name,
                              required: true,
                              autofocus: true,
                              label: "Nombre",
                              input_html: { autocomplete: "name" } %>
            <%= f.input :last_name,
                              required: true,
                              autofocus: true,
                              label: "Apellido",
                              input_html: { autocomplete: "name" } %>
            <%= f.input :email,
                        required: true,
                        autofocus: true,
                        input_html: { autocomplete: "email" }%>
            <%= f.input :password,
                        required: true,
                        hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                        input_html: { autocomplete: "new-password" } %>
            <%= f.input :password_confirmation,
                        required: true,
                        input_html: { autocomplete: "new-password" } %>
            <%= f.input :photo, input_html: { class: 'd-none' }, label_html: { class: 'upload-photo'},
                          label: '📷 Upload a photo' %>

          </div>

          <div class="form-inputs">
            <%= f.input :email,
                        required: true,
                        autofocus: true,
                        input_html: { autocomplete: "email" }%>
            <%= f.input :password,
                        required: true,
                        hint: ("#{@minimum_password_length} mínimo de caracteres" if @minimum_password_length),
                        input_html: { autocomplete: "new-password" },
                        label: "Ingresa tu contraseña" %>
            <%= f.input :password_confirmation,
                        required: true,
                        input_html: { autocomplete: "new-password" },
                        label: "Confirma tu contraseña" %>
          </div>

          <div class="form-actions text-center mb-3">
            <%= f.button :submit, "Registrate", class: "btn btn-primary"%>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
      </div>
    </div>
    <div class="col-3"></div>
  </div>
</div>
